/* eslint-disable */
import { getAPIClient } from "./axios";

export const api = getAPIClient();

// vamos utilizr o axios pra enviar o token quando fazermos as requiscoes pro backend
// we use axios to send the token when we do backend requisitions
